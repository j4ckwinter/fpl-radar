/** Default cap on total candidates returned (avoids unbounded output). */
export const DEFAULT_MAX_CANDIDATES = 2000;

/** Default per-position limit for IN pool when narrowing (v1 heuristic). */
export const DEFAULT_IN_POOL_PER_POSITION = 300;
