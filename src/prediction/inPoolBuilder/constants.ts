/** FPL status for unavailable players; excluded from in-pool. */
export const STATUS_UNAVAILABLE = "u";

/** Value used for selectedByPercent when null (sort puts nulls last when descending). */
export const SELECTED_BY_PERCENT_NULL_FALLBACK = -1;
