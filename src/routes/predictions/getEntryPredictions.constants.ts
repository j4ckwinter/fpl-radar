/** Default limit for predictions returned per request. */
export const DEFAULT_LIMIT = 20;

/** Maximum allowed limit for predictions per request. */
export const MAX_LIMIT = 100;

/** Cache TTL in seconds for entry predictions response. */
export const PREDICTIONS_CACHE_TTL_SECONDS = 60;
